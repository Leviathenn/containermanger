/**
 * @author Leviathenn
 */
import { prompt, promptCL } from "readline-sync";

export class create{
    constructor(promted: string) {

    }
    create(){
    
    }
    private prompts() {
        let dprompt = prompt({prompt: "create> "})
    }
}

